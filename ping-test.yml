---
- name: ping all hosts
  gather_facts: false
  hosts: all
  tasks:
    - name: ping a host
      ping:

- name: commvault status
  gather_facts: false
  hosts: backupProxyServer:mediaAgentServer
  tasks:
    - name: CLMP port status
      shell: timeout 5 bash -c 'cat < /dev/null > /dev/udp/10.2.122.178/389'; echo $?
      
